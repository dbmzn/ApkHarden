<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".BusinessApplication"
        android:appComponentFactory=".FixtureAppComponentFactory"
        android:label="Fixture">
        <provider
            android:name=".FixtureProvider"
            android:authorities="${applicationId}.fixture-provider"
            android:exported="false"
            android:process=":worker" />
    </application>
</manifest>
